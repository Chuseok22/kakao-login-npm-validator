import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.kakaonpmvalidator',
  appName: 'KakaoNpmValidator',
  webDir: 'dist',
};

export default config;
